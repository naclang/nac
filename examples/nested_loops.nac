// Example: Nested Loops (Multiplication Table)
// ---------------------------------------------------

out("Çarpım Tablosu (1-5):");
for(row = 1; row <= 5; row++) {
    for(col = 1; col <= 5; col++) {
        product = row * col;
        out(product);
    };
    out("---");
};